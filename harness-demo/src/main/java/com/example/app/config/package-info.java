/**
 * Spring 配置层。
 * <p>
 * 包含 {@code @Configuration} 配置类、{@code @ConfigurationProperties}、
 * {@code @MapperScan}、{@code MybatisPlusInterceptor} 等。
 * 仅依赖 {@link com.example.app.domain}。
 */
package com.example.app.config;
