/**
 * MyBatis-Plus Entity 与 Value Object。
 * <p>
 * Entity 使用 {@code @TableName} / {@code @TableId} / {@code @TableField} 注解。
 * 不允许引用 service / mapper / controller 层。
 */
package com.example.app.domain.model;
